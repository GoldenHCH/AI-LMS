{
  "name": "ai-lms",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "description": "AI-Native Course Editor for Canvas — Supabase connection",
  "scripts": {
    "check:supabase": "node --env-file=.env scripts/check-supabase.mjs",
    "test": "node --test \"tests/**/*.test.mjs\"",
    "check:tests": "node scripts/check-test-discrimination.mjs"
  },
  "dependencies": {
    "@supabase/supabase-js": "^2.58.0"
  }
}
